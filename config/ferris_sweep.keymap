/*
 * Copyright (c) 2024 The ZMK Contributors
 *
 * SPDX-License-Identifier: MIT
 */

#include <behaviors.dtsi>
#include <dt-bindings/zmk/keys.h>
#include <dt-bindings/zmk/bt.h>

#define BASE 0
#define LOWER 1
#define NAV 2
#define ADJUST 3

/ {
    behaviors {
        hm: homerow_mods {
            compatible = "zmk,behavior-hold-tap";
            label = "HOMEROW_MODS";
            #binding-cells = <2>;
            tapping-term-ms = <200>;
            quick-tap-ms = <500>;
            flavor = "tap-preferred";
            bindings = <&kp>, <&kp>;
        };
    };

    conditional_layers {
        compatible = "zmk,conditional-layers";
        tri_layer {
            if-layers = <LOWER NAV>;
            then-layer = <ADJUST>;
        };
    };

    combos {
        compatible = "zmk,combos";
        combo_minus {
            timeout-ms = <100>;
            key-positions = <8 23>;
            bindings = <&kp MINUS>;
        };
        combo_equal {
            timeout-ms = <100>;
            key-positions = <23 24>;
            bindings = <&kp EQUAL>;
        };
        combo_quote {
            timeout-ms = <100>;
            key-positions = <7 24>;
            bindings = <&kp SQT>;
        };
        combo_plus {
            timeout-ms = <50>;
            key-positions = <2 3>;
            bindings = <&kp PLUS>;
        };
        combo_tmux {
            timeout-ms = <200>;
            key-positions = <15 32>;
            bindings = <&kp LC(B)>;
        };
        combo_bkspace {
            timeout-ms = <100>;
            key-positions = <18 19>;
            bindings = <&kp BSPC>;
        };
    };

    keymap {
        compatible = "zmk,keymap";

        default_layer {
            bindings = <
                &kp Q        &kp W      &kp E        &kp R      &kp T         &kp Y      &kp U      &kp I        &kp O       &kp P
                &hm LALT A   &hm LCTRL S &hm LSHIFT D &hm LGUI F &kp G         &kp H      &hm RGUI J &hm RSHIFT K &hm RCTRL L &hm RALT SEMICOLON
                &kp Z        &kp X      &kp C        &kp V      &kp B         &kp N      &kp M      &kp COMMA    &kp DOT     &kp FSLH
                                           &lt NAV ENTER &lt LOWER SPACE       &lt LOWER BSPC &lt NAV ESC
            >;
        };

        lower_layer {
            bindings = <
                &trans       &trans     &trans       &kp F11    &kp F12       &kp GRAVE  &trans     &trans       &kp LBKT   &kp RBKT
                &hm LALT F10 &hm LCTRL F9 &hm LSHIFT F8 &hm LGUI F7 &kp F6   &kp N1     &hm RGUI N2 &hm RSHIFT N3 &hm RCTRL N4 &hm RALT N5
                &kp F5       &kp F4     &kp F3       &kp F2     &kp F1        &kp N6     &kp N7     &kp N8       &kp N9     &kp N0
                                              &kp ENTER   &kp SPACE            &kp BSPC   &kp ESC
            >;
        };

        nav_layer {
            bindings = <
                &trans       &trans     &trans       &trans     &trans        &trans     &trans     &trans       &trans     &trans
                &trans       &trans     &trans       &trans     &trans        &trans     &trans     &trans       &trans     &trans
                &trans       &trans     &trans       &trans     &trans        &trans     &trans     &trans       &trans     &trans
                                              &trans     &trans                &trans     &trans
            >;
        };

        adjust_layer {
            bindings = <
                &trans       &trans     &trans       &trans     &trans        &trans     &trans     &trans       &trans     &trans
                &trans       &trans     &trans       &trans     &trans        &trans     &trans     &trans       &trans     &trans
                &trans       &trans     &trans       &trans     &trans        &trans     &trans     &trans       &trans     &trans
                                              &trans     &trans                &trans     &trans
            >;
        };
    };
};
